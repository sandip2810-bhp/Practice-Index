function subtract(a, b) {
    return a - b;
}

module.exports = {subtract};

// learn how many types of ports(not more than 3 min) are there in computer science also learn http module (not shorter than 10 min)
 